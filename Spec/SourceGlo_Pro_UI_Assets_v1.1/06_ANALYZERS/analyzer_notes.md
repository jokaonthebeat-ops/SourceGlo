# Analyzer Assets

The grid images are reusable backgrounds. Draw the live source/reference traces, filled areas, labels, conflict range, radar polygons, and score bars in JUCE. Do not stretch the grid nonuniformly.
